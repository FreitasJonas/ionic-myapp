export class Documento {

    constructor(
        docNome: string,
        docObrigatorio: boolean,
        docFileName: string,
        docFileBase64: string,
        docFileHash: string,
        docFileTam: number,
        docFilePaginas: number) {
    }
}